PORT=3000

# Supabase (project: JibuWhatsapp / dmhqqfxlmvaeeyxdlthd)
# The server refuses to start unless all three are set.
SUPABASE_URL=https://dmhqqfxlmvaeeyxdlthd.supabase.co
SUPABASE_ANON_KEY=            # safe for the browser; RLS governs it
SUPABASE_SERVICE_ROLE_KEY=    # server only, never sent to the browser

# n8n inbound endpoint that Telegram webhooks are registered against.
N8N_TELEGRAM_WEBHOOK_URL=https://blueberrymangoes.app.n8n.cloud/webhook/telegram-in

# Optional: comma separated allowed origins. Defaults to reflecting the request origin.
CORS_ORIGIN=
